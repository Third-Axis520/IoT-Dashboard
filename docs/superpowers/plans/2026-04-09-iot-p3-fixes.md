# IoT Dashboard P3 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans

**Goal:** 三個 P3 小優化：DB 索引補強（RegisterMapEntries 唯一 + HasMaterial 索引）、SensorReadings 資料清理 endpoint（防 DB 無限成長）、OpenTelemetry 基礎監控。

**Architecture:** 純後端。OpenTelemetry 加 Console exporter（開發可視），生產可換 OTLP。資料清理用 DELETE WHERE Timestamp < cutoff，不影響現有功能。

**Tech Stack:** .NET 9, EF Core 9, OpenTelemetry .NET SDK

---

## File Structure

- Modify: `backend/Data/IoTDbContext.cs` — RegisterMapEntries 加 IsUnique
- Modify: `backend/Program.cs` — DDL 補 HasMaterial 索引 + OpenTelemetry 服務
- Create: `backend/Controllers/MaintenanceController.cs` — 資料清理 endpoint
- Modify: `backend/IoT.CentralApi.csproj` — 加 OpenTelemetry NuGet

---

## Task 1: DB 索引補強

**Files:**
- Modify: `backend/Data/IoTDbContext.cs`
- Modify: `backend/Program.cs`

**問題 1：** `RegisterMapEntries (ProfileId, RegisterAddress)` 目前無唯一約束，理論上同一 Profile 可存兩筆相同地址。

**問題 2：** `SensorReadings` 有 `(AssetCode, SensorId, Timestamp)` 索引，但 `HasMaterial` 欄位無索引，若要查「有料的歷史」需全表掃描。

- [ ] **Step 1: 讀 `IoTDbContext.cs` 確認現有索引**

確認第 45 行：
```csharp
modelBuilder.Entity<RegisterMapEntry>()
    .HasIndex(e => new { e.ProfileId, e.RegisterAddress });
```
此索引**沒有** `.IsUnique()`。

- [ ] **Step 2: 修改 RegisterMapEntries 索引加 IsUnique**

```csharp
// 改前
modelBuilder.Entity<RegisterMapEntry>()
    .HasIndex(e => new { e.ProfileId, e.RegisterAddress });

// 改後
modelBuilder.Entity<RegisterMapEntry>()
    .HasIndex(e => new { e.ProfileId, e.RegisterAddress })
    .IsUnique();
```

- [ ] **Step 3: 在 Program.cs 的 DDL 區塊補 HasMaterial 索引**

找到 `HasMaterial` 的 ALTER TABLE 區塊（約第 180 行左右），在其 `END` 後補一個新的 `ExecuteSqlRawAsync`：

```csharp
    // SensorReadings HasMaterial + AssetCode 複合索引（查詢有料歷史用）
    await ctx.Database.ExecuteSqlRawAsync("""
        IF NOT EXISTS (
            SELECT 1 FROM sys.indexes
            WHERE object_id = OBJECT_ID('dbo.SensorReadings')
              AND name = 'IX_SensorReadings_AssetCode_HasMaterial_Timestamp'
        )
        BEGIN
            CREATE INDEX [IX_SensorReadings_AssetCode_HasMaterial_Timestamp]
                ON [dbo].[SensorReadings] ([AssetCode], [HasMaterial], [Timestamp] DESC);
        END
        """);
```

- [ ] **Step 4: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: 0 errors

- [ ] **Step 5: Commit**

```bash
git add backend/Data/IoTDbContext.cs backend/Program.cs
git commit -m "fix: RegisterMapEntries unique index + HasMaterial query index"
```

---

## Task 2: SensorReadings 資料清理 endpoint

**Files:**
- Create: `backend/Controllers/MaintenanceController.cs`

**背景：** SensorReadings 每次 ingest 都寫入（1Hz × N 台設備 × 24hr = 數十萬筆/天）。無清理機制會讓 DB 無限成長。提供一個 DELETE endpoint，讓管理員手動觸發或定期排程。

- [ ] **Step 1: 建立 `MaintenanceController.cs`**

```csharp
using IoT.CentralApi.Data;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace IoT.CentralApi.Controllers;

[ApiController]
[Route("api/maintenance")]
public class MaintenanceController(IDbContextFactory<IoTDbContext> dbFactory) : ControllerBase
{
    /// <summary>
    /// 清除指定天數前的 SensorReadings 資料。
    /// 預設保留最近 30 天，最少保留 7 天。
    /// </summary>
    [HttpDelete("sensor-readings")]
    public async Task<IActionResult> PurgeSensorReadings([FromQuery] int keepDays = 30)
    {
        keepDays = Math.Max(keepDays, 7); // 最少保留 7 天
        var cutoff = DateTime.UtcNow.AddDays(-keepDays);

        await using var db = await dbFactory.CreateDbContextAsync();

        var deleted = await db.SensorReadings
            .Where(r => r.Timestamp < cutoff)
            .ExecuteDeleteAsync();

        return Ok(new
        {
            deleted,
            cutoff = cutoff.ToString("yyyy-MM-dd HH:mm:ss UTC"),
            keepDays
        });
    }

    /// <summary>
    /// 清除指定天數前已確認的 SensorAlerts。
    /// 預設保留最近 90 天，最少保留 30 天。
    /// </summary>
    [HttpDelete("alerts")]
    public async Task<IActionResult> PurgeAlerts([FromQuery] int keepDays = 90)
    {
        keepDays = Math.Max(keepDays, 30);
        var cutoff = DateTime.UtcNow.AddDays(-keepDays);

        await using var db = await dbFactory.CreateDbContextAsync();

        var deleted = await db.SensorAlerts
            .Where(a => a.Timestamp < cutoff && a.IsAcknowledged)
            .ExecuteDeleteAsync();

        return Ok(new
        {
            deleted,
            cutoff = cutoff.ToString("yyyy-MM-dd HH:mm:ss UTC"),
            keepDays,
            note = "僅刪除已確認（IsAcknowledged=true）的告警"
        });
    }

    /// <summary>查詢各資料表的大概筆數（用於評估何時需要清理）。</summary>
    [HttpGet("stats")]
    public async Task<IActionResult> GetStats()
    {
        await using var db = await dbFactory.CreateDbContextAsync();

        var readingsCount = await db.SensorReadings.CountAsync();
        var alertsCount   = await db.SensorAlerts.CountAsync();
        var unackAlerts   = await db.SensorAlerts.CountAsync(a => !a.IsAcknowledged);

        return Ok(new
        {
            sensorReadings = readingsCount,
            sensorAlerts   = alertsCount,
            unacknowledgedAlerts = unackAlerts
        });
    }
}
```

- [ ] **Step 2: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
git add backend/Controllers/MaintenanceController.cs
git commit -m "feat: MaintenanceController for SensorReadings/Alerts data purge"
```

---

## Task 3: OpenTelemetry 基礎監控

**Files:**
- Modify: `backend/IoT.CentralApi.csproj`
- Modify: `backend/Program.cs`

**背景：** 加入 OpenTelemetry，自動追蹤每個 HTTP request 的 latency + DB query 時間。開發環境輸出到 Console；生產環境可換 OTLP exporter（Grafana/Jaeger）。

- [ ] **Step 1: 加 NuGet 套件**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend"
dotnet add package OpenTelemetry.Extensions.Hosting --version 1.11.2
dotnet add package OpenTelemetry.Instrumentation.AspNetCore --version 1.11.1
dotnet add package OpenTelemetry.Instrumentation.EntityFrameworkCore --version 1.11.0-beta.3
dotnet add package OpenTelemetry.Exporter.Console --version 1.11.2
```

- [ ] **Step 2: 在 Program.cs 加 OpenTelemetry 服務**

找到 `builder.Services.AddRateLimiter(...)` 區塊，在其**後面**加入：

```csharp
// ── OpenTelemetry 監控（開發：Console；生產：換 OTLP）─────────────────────
builder.Services.AddOpenTelemetry()
    .WithTracing(tracing => tracing
        .AddAspNetCoreInstrumentation(opts =>
        {
            opts.RecordException = true;
            opts.Filter = ctx =>
                !ctx.Request.Path.StartsWithSegments("/openapi") &&
                !ctx.Request.Path.StartsWithSegments("/swagger");
        })
        .AddEntityFrameworkCoreInstrumentation(opts =>
        {
            opts.SetDbStatementForText = true;
        })
        .AddConsoleExporter())
    .WithMetrics(metrics => metrics
        .AddAspNetCoreInstrumentation()
        .AddConsoleExporter());
```

- [ ] **Step 3: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: 0 errors（若有 beta package warning 可忽略）

- [ ] **Step 4: Commit**

```bash
git add backend/IoT.CentralApi.csproj backend/Program.cs
git commit -m "feat: add OpenTelemetry tracing + metrics (Console exporter for dev)"
```

---

## Task 4: 整合驗證

- [ ] **Step 1: Final build**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: 0 errors

- [ ] **Step 2: 確認 git log**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard" && git log --oneline -10
```

Expected: 看到 P0 + P1 + P2 + P3 共 7+ commits

---

## Self-Review

- [x] Task 1：RegisterMapEntries IsUnique + HasMaterial index ✓
- [x] Task 2：MaintenanceController 3 endpoints (purge readings / purge alerts / stats) ✓
- [x] Task 3：OpenTelemetry HTTP + EF Core tracing ✓
- [x] 無 TBD placeholder ✓
- [x] keepDays 防呆（最少 7 天 / 30 天）確保不誤刪近期資料 ✓
- [x] OTel filter 排除 /openapi + /swagger 路徑，避免 trace 雜訊 ✓
