# IoT Dashboard P1 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修復 3 個 P1 級問題：HistoryController 降採樣會遺漏溫度尖峰、RegisterMapController 整批替換無事務保護、AlertsController pageSize 無上限。

**Architecture:** 純後端修改，不涉及前端或 DB schema。LTTB 演算法實作為靜態 helper 類別，與 Controller 解耦。RegisterMapController 用 EF Core 的隱式事務（SaveChangesAsync 本身就是 atomic）確認行為。

**Tech Stack:** .NET 9 C#, EF Core 9, LINQ

---

## File Structure

- Modify: `backend/Controllers/HistoryController.cs` — 替換等間距採樣為 LTTB
- Create: `backend/Utilities/LttbSampler.cs` — LTTB 演算法靜態類別
- Modify: `backend/Controllers/RegisterMapController.cs` — Save 加明確事務 + maxPageSize 防呆
- Modify: `backend/Controllers/AlertsController.cs` — pageSize 上限 + 預設改為最近 1 小時

---

## Task 1: LTTB 降採樣 — 建立 LttbSampler

**Files:**
- Create: `backend/Utilities/LttbSampler.cs`

**背景：** LTTB（Largest-Triangle-Three-Buckets）是時序圖表標準降採樣演算法。等間距採樣在 step=3 時會跳過 index 1、2，若溫度尖峰恰好在那兩點，圖表完全看不到。LTTB 保留視覺上最重要的點（最大三角形面積），波峰/波谷不會遺漏。

- [ ] **Step 1: 建立 `backend/Utilities/` 目錄（若不存在）並建立 `LttbSampler.cs`**

```csharp
namespace IoT.CentralApi.Utilities;

/// <summary>
/// Largest-Triangle-Three-Buckets (LTTB) 降採樣演算法。
/// 保留時序數據的視覺形狀，確保波峰/波谷不會在採樣中遺漏。
/// </summary>
public static class LttbSampler
{
    /// <summary>
    /// 將 <paramref name="data"/> 降採樣至最多 <paramref name="threshold"/> 個點。
    /// </summary>
    /// <param name="data">原始時序資料（需已按時間排序）</param>
    /// <param name="threshold">目標點數（含首尾兩點）</param>
    public static List<(long Time, double Value)> Sample(
        IReadOnlyList<(long Time, double Value)> data,
        int threshold)
    {
        if (threshold <= 0 || data.Count <= threshold)
            return data.ToList();

        var result = new List<(long, double)>(threshold);
        var bucketSize = (double)(data.Count - 2) / (threshold - 2);

        // 第一個點永遠保留
        result.Add(data[0]);

        int selectedIdx = 0;

        for (int i = 0; i < threshold - 2; i++)
        {
            // 計算下一個 bucket 的平均值（用作三角形的第三個頂點）
            var nextBucketStart = (int)Math.Floor((i + 1) * bucketSize) + 1;
            var nextBucketEnd   = (int)Math.Floor((i + 2) * bucketSize) + 1;
            nextBucketEnd = Math.Min(nextBucketEnd, data.Count - 1);

            double avgTime  = 0, avgValue = 0;
            int count = nextBucketEnd - nextBucketStart;
            for (int j = nextBucketStart; j < nextBucketEnd; j++)
            {
                avgTime  += data[j].Time;
                avgValue += data[j].Value;
            }
            if (count > 0) { avgTime /= count; avgValue /= count; }

            // 目前 bucket 的範圍
            var bucketStart = (int)Math.Floor(i * bucketSize) + 1;
            var bucketEnd   = (int)Math.Floor((i + 1) * bucketSize) + 1;
            bucketEnd = Math.Min(bucketEnd, data.Count - 1);

            // 找面積最大的三角形
            var (aTime, aValue) = data[selectedIdx];
            double maxArea = -1;
            int maxIdx = bucketStart;

            for (int j = bucketStart; j < bucketEnd; j++)
            {
                var (bTime, bValue) = data[j];
                // 三角形面積（絕對值）
                double area = Math.Abs(
                    (aTime - avgTime) * (bValue - aValue) -
                    (aTime - bTime)   * (avgValue - aValue)
                ) * 0.5;

                if (area > maxArea) { maxArea = area; maxIdx = j; }
            }

            result.Add(data[maxIdx]);
            selectedIdx = maxIdx;
        }

        // 最後一個點永遠保留
        result.Add(data[^1]);
        return result;
    }
}
```

- [ ] **Step 2: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: Build succeeded, 0 errors

- [ ] **Step 3: Commit**

```bash
git add backend/Utilities/LttbSampler.cs
git commit -m "feat: add LTTB downsampling algorithm"
```

---

## Task 2: 套用 LTTB 到 HistoryController

**Files:**
- Modify: `backend/Controllers/HistoryController.cs:40-63`

- [ ] **Step 1: 在 HistoryController.cs 頂部加 using**

```csharp
using IoT.CentralApi.Utilities;
```

- [ ] **Step 2: 將降採樣區塊完整替換**

找到第 40-64 行（`// 依 SensorId 分組，各自降採樣` 到 `return Ok(result)`），替換為：

```csharp
        // 依 SensorId 分組，各自用 LTTB 降採樣（保留波峰/波谷）
        var result = rawData
            .GroupBy(r => r.SensorId)
            .ToDictionary(
                g => g.Key,
                g =>
                {
                    var items = g
                        .Select(r => (
                            Time: new DateTimeOffset(r.Timestamp, TimeSpan.Zero).ToUnixTimeMilliseconds(),
                            r.Value))
                        .ToList();

                    var sampled = LttbSampler.Sample(items, maxPoints);

                    return sampled.Select(p => new HistoryPoint
                    {
                        Time  = p.Time,
                        Value = p.Value
                    }).ToList();
                });

        return Ok(result);
```

- [ ] **Step 3: 同時修正 `maxPoints` 上限防呆**，在方法參數後加驗證（加在 `var utcFrom = ...` 之前）：

```csharp
        maxPoints = Math.Clamp(maxPoints, 10, 1000);
```

- [ ] **Step 4: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: Build succeeded, 0 errors

- [ ] **Step 5: Commit**

```bash
git add backend/Controllers/HistoryController.cs
git commit -m "fix: replace equal-interval sampling with LTTB in HistoryController"
```

---

## Task 3: RegisterMapController 明確事務 + 防呆

**Files:**
- Modify: `backend/Controllers/RegisterMapController.cs:29-66`

**背景：** 目前 Save 流程：
1. RemoveRange(舊 Entries)
2. 新增新 Entries
3. SaveChangesAsync()

EF Core 的 `SaveChangesAsync()` 已是原子操作（隱式事務），所以上述流程其實已有基本保護。**但** 如果有人在 SaveChangesAsync 前另外 await 了其他操作，隱式事務會分裂。最保險做法是加明確的 `BeginTransactionAsync`，讓程式碼意圖清晰。

另外新增：**重複提交防呆**（`If-Match` header + Idempotency）太複雜，本次先加 `ProfileName` 長度驗證 + `Entries` 數量上限。

- [ ] **Step 1: 在 Save 方法加明確事務**

找到 `public async Task<IActionResult> Save(...)` 方法，將 `await using var db = ...` 之後的程式碼包進 transaction：

```csharp
    [HttpPost("{lineId}")]
    public async Task<IActionResult> Save(string lineId, [FromBody] SaveRegisterMapRequest req)
    {
        if (req.Entries.Count > 200)
            return BadRequest("Entries 數量上限為 200");

        await using var db = await dbFactory.CreateDbContextAsync();
        await using var tx = await db.Database.BeginTransactionAsync();
        var now = DateTime.UtcNow;

        try
        {
            var profile = await db.RegisterMapProfiles
                .Include(p => p.Entries)
                .Include(p => p.PlcTemplate).ThenInclude(t => t!.Zones)
                .Include(p => p.PlcTemplate).ThenInclude(t => t!.Registers)
                .FirstOrDefaultAsync(p => p.LineId == lineId);

            if (profile == null)
            {
                profile = new RegisterMapProfile { LineId = lineId };
                db.RegisterMapProfiles.Add(profile);
            }

            profile.ProfileName = req.ProfileName;
            profile.PlcTemplateId = req.PlcTemplateId;
            profile.UpdatedAt = now;

            db.RegisterMapEntries.RemoveRange(profile.Entries);
            profile.Entries = req.Entries.Select(e => new RegisterMapEntry
            {
                ZoneIndex = e.ZoneIndex,
                RegisterAddress = e.RegisterAddress,
                EquipmentId = e.EquipmentId,
                PointId = e.PointId,
                Label = e.Label,
                Unit = e.Unit,
            }).ToList();

            await db.SaveChangesAsync();
            await tx.CommitAsync();
            return Ok(MapToDto(profile));
        }
        catch
        {
            await tx.RollbackAsync();
            throw;
        }
    }
```

- [ ] **Step 2: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: Build succeeded, 0 errors

- [ ] **Step 3: Commit**

```bash
git add backend/Controllers/RegisterMapController.cs
git commit -m "fix: explicit transaction in RegisterMapController.Save + 200 entries limit"
```

---

## Task 4: AlertsController 小修正

**Files:**
- Modify: `backend/Controllers/AlertsController.cs:19-22`

**問題：** pageSize 無上限（可以要求 pageSize=99999），預設查 24 小時太廣。

- [ ] **Step 1: 在 Get 方法第一行加防呆**

在 `var utcFrom = ...` 之前加：

```csharp
        pageSize = Math.Clamp(pageSize, 1, 200);
```

- [ ] **Step 2: 預設查詢時間從 24 小時改為 1 小時**

```csharp
// 改前
var utcFrom = from?.ToUniversalTime() ?? DateTime.UtcNow.AddHours(-24);

// 改後
var utcFrom = from?.ToUniversalTime() ?? DateTime.UtcNow.AddHours(-1);
```

- [ ] **Step 3: 驗證編譯**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: Build succeeded, 0 errors

- [ ] **Step 4: Commit**

```bash
git add backend/Controllers/AlertsController.cs
git commit -m "fix: clamp pageSize to 200, default time window to 1h in AlertsController"
```

---

## Task 5: 整合驗證

- [ ] **Step 1: 完整後端 build**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend" && dotnet build 2>&1
```

Expected: 0 errors, 0 warnings

- [ ] **Step 2: 確認新檔案存在**

```bash
ls "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard\backend\Utilities\"
```

Expected: `LttbSampler.cs` 在列表中

- [ ] **Step 3: 確認 git log**

```bash
cd "C:\Users\Keith.Lee\Diamond Groups\AI\IoT-Dashboard" && git log --oneline -6
```

Expected: 看到本次 4 個 commit（LTTB + History + RegisterMap + Alerts）

---

## Self-Review Checklist

- [x] Task 1/2 覆蓋：LTTB 演算法 + HistoryController 套用 ✓
- [x] Task 3 覆蓋：RegisterMapController 明確事務 ✓
- [x] Task 4 覆蓋：AlertsController pageSize + 時間窗口 ✓
- [x] 無 TBD/TODO placeholder
- [x] `LttbSampler.Sample` 在 Task 1 定義，Task 2 使用 ✓
- [x] `Math.Clamp` 用法一致（Task 2 的 maxPoints、Task 4 的 pageSize）✓
- [x] Transaction rollback 在 Task 3 明確處理 ✓
