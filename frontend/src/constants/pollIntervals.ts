/** Selectable polling intervals (seconds) shown in connection forms.
 *  Used by both the integration wizard (Step2_Config) and the edit modal. */
export const POLL_INTERVAL_SECONDS = [1, 2, 5, 10, 30, 60] as const;
